import DiscoveryLayout from "@/components/discovery/DiscoveryLayout";

export default function DiscoverySectionLayout({ children }: { children: React.ReactNode }) {
  return <DiscoveryLayout>{children}</DiscoveryLayout>;
}
